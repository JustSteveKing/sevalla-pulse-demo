services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      APP_KEY: "${APP_KEY}"
      APP_ENV: local
      APP_DEBUG: "true"
    volumes:
      - .:/app